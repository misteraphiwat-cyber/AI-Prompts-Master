rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /prompts/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && 
                   get(/databases/(default)/documents/profiles/$(request.auth.uid)).data.status == 'admin';
    }
  }
}
